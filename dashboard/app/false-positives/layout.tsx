export const metadata = {
  title: "False Positive Analysis | SC Audit Benchmark",
  description: "Which models waste auditor time? Precision metrics and cost impact per vulnerability class.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
