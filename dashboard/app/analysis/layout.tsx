export const metadata = {
  title: "Evidence | SC Audit Benchmark",
  description: "Per-SWC rationale: which LLMs pass, which fail, and the architectural reason behind the split.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
