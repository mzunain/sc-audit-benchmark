export const metadata = {
  title: "Proof Lab | SC Audit Benchmark",
  description: "Executable Foundry harnesses for all 8 SWC vulnerability classes. Reproduce, patch, verify.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
