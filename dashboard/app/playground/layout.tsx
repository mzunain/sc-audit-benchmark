export const metadata = {
  title: "Playground | SC Audit Benchmark",
  description: "Paste a Solidity contract, pick a model, get a structured vulnerability scan report.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
