export const metadata = {
  title: "Coverage Heatmap | SC Audit Benchmark",
  description: "Per-model × per-SWC detection heatmap. Click any cell to inspect the exact scanner output.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
