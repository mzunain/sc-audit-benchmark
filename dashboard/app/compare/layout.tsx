export const metadata = {
  title: "Model Comparison | SC Audit Benchmark",
  description: "Side-by-side comparison of LLM auditors: detection rates, false positives, per-SWC coverage, costs, strengths and weaknesses.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
