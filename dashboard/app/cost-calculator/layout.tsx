export const metadata = {
  title: "Cost Calculator | SC Audit Benchmark",
  description: "Calculate the true cost of AI-assisted smart contract auditing: LLM fees + false positive triage + missed vulnerability risk.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
