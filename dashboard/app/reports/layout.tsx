export const metadata = {
  title: "Audit Reports | SC Audit Benchmark",
  description: "Generate professional audit reports with consensus signals and remediation priorities.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
