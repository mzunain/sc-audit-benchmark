export const metadata = {
  title: "Risk Matrix | SC Audit Benchmark",
  description: "Multi-model consensus scoring. Know which contracts need 3-model consensus.",
};
export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
